<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.offsidegame.offside">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/football_48"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewPlayerScoreActivity" />
        <activity
            android:name=".AnswerQuestionActivity"
            android:label="@string/ask_question" />

        <service
            android:name=".helpers.SignalRService"
            android:exported="false" />

        <activity
            android:name=".LoginActivity"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".JoinGameActivity" />
        <activity android:name=".ViewProcessedQuestionActivity" />
        <activity android:name=".ViewClosedQuestionActivity"></activity>
    </application>

</manifest>